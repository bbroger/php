<?php

require(config.php)

timer  (5 segundos)
if (require_once(arquivo gerado no java com extensão php)){
teste = new Usuario();
if ($teste->LoadbyId($id)){ // << id que vem no arquivo // se retornar verdadeiro (conseguiu buscar) ele entra no if
require_once(insereDados.php);
}
else(){
echo "Usuario não cadastrado";
unlink(arquivo  gerado pelo java com extensão php);
}
} 
?>

<?php

arquivo insere dados \/
$contarequire(config.php)dor = 0;

if (require_once(arquivo gerado no java com extensão php) && $contador == 0) {
teste = new Usuario();
chama o insert na tabela (entrada e saida) e tal... 
select * from (tabela de clientes) where idfilho = "variavel trazida no documento"
ai nessa tabela vai ter o login e senha do pai, vc tem que ver como mandar a msg de entrada e saida especificamente pra quem tiver esse login

$contador++;

unlink(arquivo com extensão php);
}

if (require_once(arquivo gerado no java com extensão php) && $contador == 1 ) {
teste1 = new Usuario();
faz o insert na tabela (entrada e saida) e tal... 
select * from (tabela de clientes) where idfilho = "variavel trazida no documento"
ai nessa tabela vai ter o login e senha do pai, vc tem que ver como mandar a msg de entrada e saida especificamente pra quem tiver esse login

$contador--;

unlink(arquivo com extensão php);

} 

//obs: até agr pensei em 3 tabelas, uma de clientes (pai) com id do filho... login e senha do pai... data de cadastro...
// segunda tabela (filho) id do filho... nome do pai... data do cadastro...
// terceira tabela, entrada e saida (com TODOS sem separar) ai na hora de mostrar no registro (no login do pai no site) vc coloca pra mostrar
// apenas onde o id dele ta na tabela filho (select * from tb_filho where idpai = ...", dê um jeito de armazenar o iddopai no ato do login e jogar pra uma variavel
//obs: o arquivo gerado pelo java (semelhante a função fopen no php) tem que ser um arquivo php e conter um json com o codigo 
//da tag... (vc tem que vincular a tag com um usuario)
//obs: no video onde ta como funciona o rfid...  e tal, n precisa nem da verificação do if, é só gerar o aarquivo com o codigo que vier e eu faço
//a verificação no php, "veja se o codigo vem msm"
//obs: ver dps como o linkedin faz pra aparecer notificações no meu celular só por eu estar no logado no site por ele. 

?>


